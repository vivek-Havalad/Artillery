config:
  target: "http://localhost:3031"
  phases:
    - duration: 10
      arrivalRate: 1
scenarios:
  - flow:
      - post:
          url: "/sample"
          headers:
           Content-Type: "application/json"
          json:
           components: []
